# แผนออกแบบ UX/UI ใหม่ — VTuber Schedule Generator

**เอกสารอ้างอิงหลัก ฉบับที่ 2** · 26 ส.ค. 2026
**สถานะ:** เคาะทิศทางแล้ว — **A+B "Quiet Studio"** (chrome เป็นกลาง + panel อบอุ่น) · ตัดทิศทาง C ทิ้ง
**ข้อจำกัดสำคัญ:** ทุกเฟสต้องจบด้วยแอปที่ยังเปิดใช้งานได้จริง ไม่มีช่วงที่แอปพัง

> เปลี่ยนจากฉบับที่ 1: บันทึกคำตอบ 6 ข้อและเคาะทิศทาง (ข้อ 3) · เพิ่มข้อค้นพบใหม่จากคำตอบข้อ 6 ที่เปลี่ยนโครงแผน (ข้อ 4) · ใส่ค่า token จริงครบทุกตัว (ข้อ 5) · สเปค primitive ครบ 5 สถานะ (ข้อ 6) · ปรับลำดับเฟสใหม่ (ข้อ 10)

---

## 1. ขอบเขตและหลักการ

1. **ขอบเขต:** ทั้งแอป — titlebar, IconRail, EditorPanel, ทั้ง 6 tabs, preview chrome, modal, toast, empty state
2. **นอกขอบเขตเด็ดขาด:** ตัวการ์ดที่เรนเดอร์ออกมา (`components/canvas/**`) ไม่แตะแม้แต่พิกเซลเดียวในโปรเจกต์นี้
3. **หลักการที่ 1 — ห้ามแตะ logic:** redesign นี้แก้เฉพาะชั้นการนำเสนอ ห้ามแก้ `store/`, `export/`, `utils/` (ยกเว้นเพิ่มไฟล์ใหม่)
4. **หลักการที่ 2 — output ต้องเหมือนเดิมเป๊ะ:** PNG/GIF ที่ export ก่อนและหลัง redesign ต้องได้ไฟล์ที่ hash ตรงกัน ใช้ `utils/renderTest.ts` เป็นตัวพิสูจน์
5. **หลักการที่ 3 — เฟสละหนึ่ง branch:** ทุกเฟสจบแล้ว merge ได้ ใช้งานได้
6. **หลักการที่ 4 — เพิ่มก่อน ลบทีหลัง:** เปลี่ยน primitive ให้เพิ่มตัวใหม่ ย้าย call site ให้หมด แล้วค่อยลบตัวเก่าใน commit แยก
7. **ข้อยกเว้นใหม่ของหลักการที่ 1:** `template/layout.schema.ts` เดิมอยู่ในรายการห้ามแตะ แต่คำตอบข้อ 6 ทำให้ต้องแตะ — อ่านเหตุผลในข้อ 4.3 การแก้จำกัดอยู่ที่ "เพิ่มฟิลด์ที่เป็น optional" เท่านั้น ห้ามเปลี่ยนหรือลบฟิลด์เดิม

---

## 2. สรุปสถานะปัจจุบัน (Audit)

### 2.1 สแตกที่ใช้อยู่

| ชั้น | ของที่ใช้ |
|---|---|
| Shell | Electron 43 + custom titlebar (frameless) |
| UI | React 19, Vite 8, Tailwind 4 (`@theme`), Radix (select/switch/accordion/tooltip/slider/dialog) |
| Canvas | Konva 10 + react-konva 19 |
| State | Zustand 5 + IndexedDB autosave + history stack ของตัวเอง |
| Icon | lucide-react + simple-icons |

### 2.2 โครงหน้าจอปัจจุบัน

```
┌───────────────────────────────────────────────────────────┐
│ titlebar h-40px  wordmark │ undo/redo │ pill │ 🌐 │ ☾ │ ─ □ ✕ │
├──────┬──────────────┬─────────────────────────────────────┤
│ rail │ EditorPanel  │ preview toolbar h-40px              │
│ 72px │ 380px (คงที่)│  1920×1080 · คลิกแถว   │ TH EN JP  │
│      │              ├─────────────────────────────────────┤
│ 6ปุ่ม │  tab content │        StageScaler (auto-fit)       │
│ help │              │                                     │
└──────┴──────────────┴─────────────────────────────────────┘
```

### 2.3 ปัญหาที่พบ 16 ข้อ เรียงตามความรุนแรง

1. **แก้วันในกล่อง modal ที่บังพรีวิว** — `DayEditPanel` เป็น `RDialog` เต็มจอ ปิดการ์ดที่กำลังแก้ทั้งใบ ผู้ใช้พิมพ์ชื่อสตรีมโดยไม่เห็นว่ามันล้นกรอบไหม ต้องปิดกล่องถึงจะรู้ นี่คือปัญหา UX ที่หนักที่สุด
2. **schema ของเทมเพลตผูกติดกับเทมเพลตเดียว** — `TemplateLayout` บังคับให้มี `images.titleFlowers`, `images.weekRibbon`, `row.ribbon`, `row.bullet`, `row.dash` ครบทุกตัว ซึ่งเป็นชิ้นส่วนเฉพาะของ Sakura Diary เทมเพลตแนวตั๋ว/แนวลิสต์จุดไข่ปลาไม่มีชิ้นส่วนพวกนี้ **ข้อนี้ขึ้นมาอันดับ 2 เพราะคำตอบข้อ 6 (จะมี 10 เทมเพลต)** — รายละเอียดในข้อ 4.3
3. **"เทมเพลต" กับ "ธีมสี" ถูกยำรวมกันในแท็บเดียว** — `TEMPLATES` มี 1 รายการ (Sakura Diary) ส่วน `THEME_PRESETS` มี 6 รายการ (Sakura Pink / Peach / Butter / Mint / Sky Blue / Lavender) ซึ่งเป็นแค่การหมุนสีของเทมเพลตเดิม พอมี 10 เทมเพลต ผู้ใช้จะแยกไม่ออกว่ากำลังเลือกอะไรอยู่
4. **ระบบสีสองชุดที่ตีกัน** — token `accent` เป็นฟ้า `#0ea5e9` และ token `gold` เป็น `#38bdf8` ซึ่ง**ก็ฟ้าเหมือนกัน** ชื่อ token ไม่ตรงกับค่าอีกต่อไป แต่ `IconRail` ยังใช้ `gold` แยกกลุ่มปุ่มอยู่
5. **เศษธีมเก่าค้างในโค้ด** — `nav-rail-btn` มีทรงไพ่ทาโรต์ (`rounded-t-[14px] rounded-b-[6px]` + gradient) และ `InfoTip` มี `fill-[#3c3860]` (ม่วงเข้ม) ทั้งคู่เป็นซากธีม Moon Priestess
6. **สีที่ hardcode ทะลุ token** — `Badge` neutral ใช้ `bg-white/8`, `Btn` ghost ใช้ `hover:bg-white/5` ทั้งสองมองไม่เห็นบนธีมสว่าง เพราะเขียนตอนที่แอปยังเป็นธีมมืดอย่างเดียว
7. **panel กว้างคงที่ 380px** — `ThemeTab` มี 4 section + slider 3 ตัว + ฟอนต์ 3 ช่อง + FontSourcePicker อัดในความกว้างนี้ ปรับไม่ได้ ขยายหน้าต่างแล้วพื้นที่ที่ได้ไปตกที่พรีวิวหมด
8. **พรีวิวไม่มีตัวควบคุมเลย** — `StageScaler` ย่อให้พอดีอย่างเดียว (`Math.min(..., 1)`) ซูมเข้าดูตัวอักษรไม่ได้ เลื่อนไม่ได้ ไม่มี fit/100% ไม่มีตารางหมากรุกดูขอบโปร่งใส
9. **ปุ่มเปลี่ยนภาษาสองอันที่ทำคนละเรื่อง** — 🌐 บน titlebar เปลี่ยนภาษา UI ส่วน TH/EN/JP บน preview toolbar เปลี่ยนภาษาบนการ์ด อยู่ห่างกันไม่ถึงหนึ่งนิ้ว ไม่มีป้ายบอกความต่าง
10. **ลำดับความสำคัญแบน** — `SectionTitle` ทุกอันเป็น uppercase 12px สี accent เหมือนกันหมด ทุกหัวข้อ "ตะโกน" เท่ากัน
11. **ไม่มีสเกลระยะห่าง** — `space-y-6`, `p-4`, `px-2.5 py-2`, `min-h-9` ปนกันตามใจ และมี override `!px-2 !py-1 !text-xs` ใน `DraftsTab` กับ `DayEditPanel` เพราะ `Btn` ไม่มี prop `size`
12. **โค้ด mobile ซ้ำซ้อน** — `App.tsx` แตกสองสาขาที่ 768px เขียน preview toolbar ซ้ำสองชุด และ state `mobileMore` ถูก set แต่ไม่มีใครอ่าน (โค้ดตาย)
13. **ปุ่ม Help ไม่ทำงาน** — `IconRail` มีปุ่ม Help ที่ไม่มี `onClick`
14. **ไม่มี empty state / first run** — เปิดแอปครั้งแรกเจอฟอร์มเปล่ากับการ์ด placeholder ไม่มีอะไรบอกว่าเริ่มตรงไหน (ข้อนี้รุนแรงขึ้นมากเพราะจะขาย — ดูข้อ 4)
15. **Export ถูกลดชั้นเป็นแท็บ** — การกด export คือปลายทางของการใช้แอป แต่อยู่ระดับเดียวกับ "ตั้งค่าทั่วไป" ไม่มีปุ่มหลักที่เห็นตลอด
16. **focus ring บังคับ radius 10px ทุกอัน** — `:focus-visible { border-radius: 10px }` ทำให้วงโฟกัสของ Chip/SegmentedControl ทรงแคปซูลออกมาเป็นสี่เหลี่ยมมนไม่ตรงรูป

### 2.4 ของที่ทำไว้ดีแล้ว ห้ามรื้อ

1. `rafThrottle` + การปัด scale เป็นขั้นละ 0.002 ใน `StageScaler` — แก้ปัญหา re-render ตอนลากขอบหน้าต่างได้ตรงจุด
2. `getTemplate()` มี fallback `?? TEMPLATES[0]` แล้ว — เอาเทมเพลตเก่าออกไม่ทำให้แอปพัง draft เก่าจะตกมาที่เทมเพลตแรกแทน
3. บล็อก `prefers-reduced-motion` ใน `index.css` เขียนถูกต้อง
4. `ErrorBoundary` ครอบทั้ง panel และ stage แยกกัน — panel พังแล้วพรีวิวยังอยู่
5. `history` + `autosave` แยกจาก UI สนิท redesign ไม่กระทบ

---

## 3. คำตอบและการเคาะทิศทาง

### 3.1 สามทิศทางที่เทียบ (สรุปย่อจากฉบับที่ 1)

| | **A · Pro Editor** | **B · Soft Studio** | **C · Themed** |
|---|---|---|---|
| แนวคิด | chrome เงียบ ผลงานเป็นพระเอก | เครื่องมือสร้างสรรค์ที่เป็นมิตร | UI เปลี่ยนสีตามธีมการ์ด |
| สีเทา | เทากลางไร้ความอิ่มตัว | เทาอมอุ่น | ตามธีม |
| ความหนาแน่น | สูง (11–13px) | กลาง (13–14px) | กลาง |
| พื้นที่พรีวิว | มากสุด | กลาง | น้อยสุด |
| ตัดสินสีการ์ด | ดีมาก | ดี | **แย่** |
| แรงงาน | ~7 วัน | ~5 วัน | ~12 วัน |

### 3.2 คำตอบทั้ง 6 ข้อ

| # | คำถาม | คำตอบ | ผลต่อการเคาะ |
|---|---|---|---|
| 1 | ใช้นานแค่ไหนต่อครั้ง | ไม่เกิน 1 ชั่วโมง | ยาวพอที่ chrome สีจัดจะทำให้ล้าตา → **A/B ได้เปรียบ** |
| 2 | ใช้คนเดียวหรือหลายคน | ใช้ทีละคนต่อเครื่อง **แต่จะขายให้หลายคนใช้** | ผู้ใช้ไม่มีคนสอน ต้องเข้าใจเอง → **B ได้เปรียบชัด** และเพิ่มงานใหม่: empty state, Help, ข้อความ error ที่คนนอกอ่านรู้เรื่อง |
| 3 | จะเพิ่มธีมอีกกี่ธีม | **เอาธีมปัจจุบันออก แล้วเพิ่มธีมใหม่แทนสูงสุด 10 รายการ** | **ตัด C ขาด** — 10 เทมเพลต × light/dark = ต้อง QA หน้าตา UI 20 รอบทุกครั้งที่เพิ่มเทมเพลต · และซากทอง/ไพ่ทาโรต์ลบทิ้งได้ฟรี ไม่มีของเก่าให้ต้องรักษา |
| 4 | จอเดียวหรือสองจอ | จอเดียว | พื้นที่มีจำกัด → **A ได้เปรียบชัด** |
| 5 | รับได้ไหมถ้าเรียบขึ้น | รับได้ แต่ยังต้องสวย | **A ล้วนไม่ผ่าน** (เย็นชาเกิน) → ต้องผสม B |
| 6 | อยากได้ผลใน กี่วัน | ไม่ตอบเป็นวัน แต่ตอบว่า **"ใช้ปุ๊บแก้นิดหน่อยแล้ว export ได้เลย เพราะจะทำ template สไตล์ต่าง ๆ ไปให้อยู่แล้ว ต่างกันแค่ renderer"** | **นี่ไม่ใช่คำตอบเรื่องเวลา แต่เป็นข้อกำหนดของตัวสินค้า** — เปลี่ยนโครงแผน ดูข้อ 4 |

### 3.3 คะแนนรวม

| เกณฑ์ | A | B | C |
|---|---|---|---|
| ใช้ยาวถึง 1 ชม. | +1 | +1 | 0 |
| ขายให้คนอื่นใช้ | +1 | +2 | +1 |
| 10 เทมเพลต | +1 | +1 | **−3** |
| จอเดียว | +2 | 0 | −1 |
| เรียบแต่ต้องสวย | +1 | +2 | +1 |
| ต้อง export ได้เร็ว | +1 | +1 | −1 |
| **รวม** | **+7** | **+7** | **−3** |

### 3.4 ทิศทางที่เคาะ — "Quiet Studio" (A+B)

1. **chrome รอบนอก (titlebar / rail / พื้นพรีวิว) ใช้ A** — เทากลางไร้ hue, ความหนาแน่นสูง, ไม่มีเงา, เส้น 1px คม สายตาจะไม่ถูกหลอกเวลาตัดสินสีการ์ด และประหยัดพื้นที่บนจอเดียว
2. **ข้างใน EditorPanel ใช้ B** — radius นุ่ม, ระยะห่างสบายตา, ตัวอักษร 13px, เงาเบาสองระดับ ผู้ใช้ที่ซื้อไปจะไม่รู้สึกว่าเจอเครื่องมือช่างที่ต้องเรียนก่อนใช้
3. **จุดเด่นที่ทำให้ "สวย" โดยไม่ใช้สี** — พื้นพรีวิวเป็นโซนมืดจมลงไป (`sunken`) การ์ดลอยขึ้นมาพร้อมเงานุ่มและตารางหมากรุกบาง ๆ รอบขอบ นี่คือภาพจำของแอป และมันทำงานเหมือนกันหมดทั้ง 10 เทมเพลต ไม่ต้อง QA ซ้ำ
4. **ตัด C ทิ้งทั้งหมด รวมถึง toggle ในเฟส 5** — เดิมเสนอไว้เป็นสวิตช์เสริม แต่ 10 เทมเพลตทำให้ต้นทุน QA ของสวิตช์นั้นแพงเกินคุณค่า
5. **สิ่งเดียวที่เหลือจาก C** — accent ของ UI ดึงมาจาก `palette.dayOnline` ของเทมเพลตที่เลือกอยู่ **เฉพาะบนแถบพรีวิวและกรอบไฮไลต์วันที่กำลังแก้เท่านั้น** ไม่ไปแตะ panel หรือ titlebar ต้นทุนเท่ากับ token เดียว ได้ความรู้สึกผูกกับผลงานโดยไม่ทำให้ตัดสินสีผิด — และถ้าคุมยาก ก็ตัดทิ้งได้โดยไม่กระทบอะไร

---

## 4. ข้อค้นพบใหม่จากคำตอบข้อ 6 — แอปนี้คือ "เลือกเทมเพลต → แก้นิดหน่อย → export"

คำตอบข้อ 6 บอกว่าเทมเพลตจะทำสำเร็จรูปมาให้แล้ว หน้าที่ของผู้ใช้คือเลือก แก้เล็กน้อย แล้ว export ไม่ใช่ออกแบบเอง **นี่เปลี่ยนน้ำหนักของแผนทั้งฉบับ** — จากเดิมที่มองว่าเป็น "เครื่องมือออกแบบ" กลายเป็น "เครื่องมือกรอกและส่งออก" ซึ่งเน้นคนละอย่าง

### 4.1 เป้าหมายใหม่ที่วัดผลได้

1. **เปิดแอปครั้งแรกถึง export ครั้งแรก ≤ 5 คลิก** — เลือกเทมเพลต → สัปดาห์เติมอัตโนมัติ → แก้ → Export
2. **การเลือกเทมเพลตต้องเป็นขั้นแรกที่เห็นชัด** ไม่ใช่ dropdown ที่ฝังอยู่กลางแท็บ Theme
3. **ทุกอย่างต้องมีค่าเริ่มต้นที่ใช้ได้เลย** — เปิดมาแล้วการ์ดต้องดูเสร็จแล้ว ไม่ใช่ต้องกรอกก่อนถึงจะเห็นอะไร
4. **ปุ่ม Export ต้องเห็นตลอดเวลา** ไม่ต้องหา

### 4.2 แกลเลอรีเทมเพลต — พื้นผิวใหม่ที่ต้องสร้าง

1. เปลี่ยนแท็บ **Theme → Template** และให้การเลือกเทมเพลตขึ้นเป็นของแรกในแท็บ
2. แสดงเป็นกริดภาพย่อ 2 คอลัมน์ (panel 380px รองรับได้) 10 รายการ ไม่ใช่ dropdown
3. ภาพย่อต้องเป็นภาพจริงของเทมเพลตนั้น สร้างล่วงหน้าเก็บใน `public/templates/<id>/thumb.webp` ไม่เรนเดอร์สดตอนเปิด (เรนเดอร์สด 10 ใบจะทำให้เปิดแอปช้า)
4. มีตัวกรองตามสไตล์ (สมุดโน้ต / ริบบิ้น / ตั๋ว / สติกเกอร์ / มินิมอล / เต็มจอ) — ตรงกับสไตล์ที่วางไว้
5. hover แล้วขยายภาพย่อ, คลิกแล้วเปลี่ยนทันที ไม่ต้องกดยืนยัน (มี undo อยู่แล้ว)
6. **เตือน:** เปลี่ยนเทมเพลตแล้วค่าที่ผู้ใช้ปรับเองบางอย่างอาจใช้ต่อไม่ได้ ต้องมี toast บอกว่าอะไรถูกรีเซ็ต ไม่ใช่เงียบ ๆ
7. แยก "เทมเพลต" (โครงสร้าง) ออกจาก "โทนสี" (hue shift) ให้ชัด — สองอย่างนี้คนละหัวข้อ ไม่ใช่รายการเดียวกัน (แก้ปัญหา 2.3.3)

### 4.3 ปัญหาที่ต้องแก้ก่อน ไม่งั้น 10 เทมเพลตจะทำไม่ได้

ตอนนี้ `TemplateLayout` บังคับให้ทุกเทมเพลตมีชิ้นส่วนครบชุดเดียวกัน:

```ts
images: {
  background; panel; frame; placeholder;
  titleFlowers;   // ← ดอกไม้หัวการ์ด — เฉพาะ Sakura Diary
  weekRibbon;     // ← ริบบิ้น "WEEK OF" — เฉพาะ Sakura Diary
  designCredit;
}
row: { dayName; bullet; dash; time; subtitle; ribbon; offlineText }
                //   ↑ จุด    ↑ ขีด            ↑ ริบบิ้นแถว — ทั้งสามเป็นของ Sakura Diary
```

เทมเพลตแนว "ตั๋วฉีก" หรือ "ลิสต์จุดไข่ปลามินิมอล" ไม่มีริบบิ้น ไม่มีดอกไม้ ถ้ายังใช้ schema นี้จะต้องใส่รูปโปร่งใส 1×1 px หลอกไปเรื่อย ๆ ซึ่งจะพังตอนเทมเพลตที่ 3–4

**ทางแก้ที่เล็กที่สุดที่พอ:**

1. เปลี่ยนชิ้นส่วนที่ไม่ใช่แกนกลางให้เป็น optional: `titleFlowers?`, `weekRibbon?`, `row.ribbon?`, `row.bullet?`, `row.dash?`
2. แกนกลางที่ทุกเทมเพลตต้องมีจริง ๆ: `background`, `canvas`, `palette`, `rowTops`, `row.dayName`, `row.time`, `row.subtitle`
3. เพิ่มฟิลด์ `controls: string[]` ในเทมเพลต — บอกว่าแท็บ Template ควรโชว์ตัวปรับอะไรบ้างสำหรับเทมเพลตนี้
4. **UI อ่านจาก `controls` ไม่ hardcode** — ไม่งั้น 10 เทมเพลตจะกลายเป็น 10 สาขาเงื่อนไขในไฟล์ `ThemeTab.tsx`
5. ระหว่างที่ `controls` ยังไม่มีในเทมเพลตใด ให้ UI ถือว่า "โชว์ทุกอย่าง" — เข้ากันได้กับของเดิม 100%
6. งานส่วนนี้ **ทำในเฟส 3.5 ก่อนแตะแท็บ Template** ประมาณ 1 วัน

### 4.4 คำถามเดียวที่ยังค้าง

**`THEME_PRESETS` 6 โทนสี (Sakura Pink / Peach / Butter / Mint / Sky Blue / Lavender) จะเป็นของกลางใช้ได้ทุกเทมเพลต หรือแต่ละเทมเพลตพกโทนสีของตัวเองมา?**

1. **ของกลาง** — 10 เทมเพลต × 6 โทน = 60 หน้าตา ผู้ใช้เลือกอิสระ แต่บางคู่จะออกมาไม่สวย (เทมเพลตโทนเข้มเจอ Butter)
2. **แต่ละเทมเพลตพกมาเอง** — คุมคุณภาพได้ทุกคู่ แต่ต้องกำหนดโทนให้ครบ 10 เทมเพลตตอนทำ
3. **ข้อเสนอ:** แบบที่ 2 พร้อมปุ่ม "โทนสีทั้งหมด" สำหรับคนที่อยากลองเอง — คุมคุณภาพเป็นค่าเริ่มต้น แต่ไม่ปิดกั้น
4. ตอบข้อนี้ก่อนเริ่มเฟส 3.5 เพราะมันเปลี่ยนรูปร่างของ `controls`

---

## 5. Design system — ค่าจริงทั้งหมด

### 5.1 Token สี — ธีมสว่าง

```css
@theme {
  /* พื้นผิว 5 ขั้น */
  --color-canvas:       #f5f5f4;  /* พื้นหลังสุด */
  --color-surface:      #ffffff;  /* panel, titlebar */
  --color-raised:       #ececea;  /* การ์ดใน panel, hover */
  --color-sunken:       #dedbd6;  /* พื้นโรงถ่ายพรีวิว */
  --color-line:         #e2e0dc;  /* เส้นแบ่งตกแต่ง */
  --color-line-strong:  #8a867f;  /* ขอบ interactive */

  /* ตัวอักษร 4 ขั้น */
  --color-ink:          #1c1b19;
  --color-ink-muted:    #57534e;
  --color-ink-faint:    #69645e;
  --color-ink-disabled: #a8a29e;

  /* accent */
  --color-accent:        #4f46e5;  /* ข้อความ/ไอคอนสถานะเลือก */
  --color-accent-strong: #4338ca;  /* พื้นปุ่มหลัก */
  --color-accent-soft:   #eef2ff;  /* พื้นอ่อน badge/chip */

  /* สถานะ */
  --color-ok:      #146c34;
  --color-warn:    #9a4708;
  --color-danger:  #b91c1c;
  --color-special: #be185d;

  /* เงา */
  --shadow-raised:  0 1px 2px rgb(0 0 0 / .06), 0 1px 3px rgb(0 0 0 / .04);
  --shadow-overlay: 0 8px 24px -6px rgb(0 0 0 / .16), 0 2px 6px -2px rgb(0 0 0 / .08);
  --shadow-stage:   0 12px 32px -10px rgb(0 0 0 / .28);
}
```

### 5.2 Token สี — ธีมมืด

```css
.dark {
  --color-canvas:       #16161a;
  --color-surface:      #1e1e23;
  --color-raised:       #2a2a30;
  --color-sunken:       #0f0f12;
  --color-line:         #34343b;
  --color-line-strong:  #6a6a75;

  --color-ink:          #f4f4f5;
  --color-ink-muted:    #b6b6bd;
  --color-ink-faint:    #9a9aa3;
  --color-ink-disabled: #5c5c65;

  --color-accent:        #8f99fa;
  --color-accent-strong: #5457e8;
  --color-accent-soft:   rgb(143 153 250 / .16);

  --color-ok:      #4ade80;
  --color-warn:    #fbbf24;
  --color-danger:  #f87171;
  --color-special: #f472b6;

  --shadow-raised:  0 1px 2px rgb(0 0 0 / .40);
  --shadow-overlay: 0 12px 32px -8px rgb(0 0 0 / .60);
  --shadow-stage:   0 16px 40px -12px rgb(0 0 0 / .70);
}
```

### 5.3 ผลตรวจ contrast (คำนวณแล้ว ไม่ใช่ประมาณ)

| คู่สี | สว่าง | มืด | เกณฑ์ | ผล |
|---|---|---|---|---|
| ink บน surface | 17.2 | 15.1 | ≥4.5 | ✓ |
| ink-muted บน surface | 7.6 | 8.2 | ≥4.5 | ✓ |
| ink-faint บน raised (แย่สุด) | 4.95 | 5.11 | ≥4.5 | ✓ |
| accent บน raised (แย่สุด) | 5.32 | 4.78 | ≥4.5 | ✓ |
| ขาวบน accent-strong | 7.90 | 5.38 | ≥4.5 | ✓ |
| ok บน raised | 5.51 | 8.19 | ≥4.5 | ✓ |
| warn บน raised | 5.41 | 8.54 | ≥4.5 | ✓ |
| danger บน raised | 5.47 | 5.16 | ≥4.5 | ✓ |
| special บน raised | 5.10 | 5.39 | ≥4.5 | ✓ |
| line-strong บน surface | 3.62 | 3.11 | ≥3.0 | ✓ |
| line บน surface | 1.32 | 1.34 | — | เส้นตกแต่งเท่านั้น |

1. **`line` ใช้เป็นเส้นแบ่งตกแต่งได้อย่างเดียว** ห้ามใช้เป็นขอบของสิ่งที่กดได้ — ขอบ interactive ต้องเป็น `line-strong` เสมอ ข้อนี้ต่างจากโค้ดปัจจุบันที่ `TextInput`/`Btn` ใช้ `border-line`
2. `ink-disabled` ไม่ต้องผ่านเกณฑ์ (WCAG ยกเว้นสถานะปิดใช้งาน) แต่ต้องยังพอมองเห็น

### 5.4 Token ระยะ ขนาด ตัวอักษร

```css
@theme {
  --space-1: 4px;   --space-2: 8px;   --space-3: 12px;
  --space-4: 16px;  --space-5: 24px;  --space-6: 32px;

  --control-sm: 28px;  --control-md: 34px;  --control-lg: 40px;

  --text-micro: 11px;  /* ตัวเลขประกอบ, counter */
  --text-label: 12px;  /* ป้ายฟิลด์ */
  --text-body:  13px;  /* เนื้อหาหลัก */
  --text-title: 15px;  /* หัวข้อ panel */
  --text-head:  18px;  /* หัวข้อหน้า */

  --radius-btn:   8px;
  --radius-card:  12px;
  --radius-panel: 16px;
  --radius-pill:  9999px;

  --font-display: "Plus Jakarta Sans", "Inter", "IBM Plex Sans Thai", system-ui, sans-serif;
  --font-ui:      "Plus Jakarta Sans", "Inter", "IBM Plex Sans Thai", system-ui, sans-serif;
  --font-mono:    "IBM Plex Mono", ui-monospace, monospace;
}
```

1. radius ลดจาก 12/16/20 เหลือ 8/12/16 — เรียบขึ้นตามคำตอบข้อ 5 แต่ยังไม่ใช่มุมเหลี่ยม
2. ห้ามใช้ค่าระยะนอกสเกล 6 ขั้น ห้ามใช้ความสูงควบคุมนอก 3 ขั้น
3. ตัวอักษรไทยกับอังกฤษต้องอยู่ในสแตกเดียวกัน อย่าแยก `font-th` เพราะจะทำให้ความสูงบรรทัดไม่ตรงกันเวลาสลับภาษา

### 5.5 กฎการใช้สีที่บังคับ

1. `accent` ใช้ได้เฉพาะ **"สิ่งที่กำลังเลือกอยู่"** กับ **"ปุ่มหลักหนึ่งปุ่มต่อหน้าจอ"** — ห้ามใช้กับหัวข้อ (แก้ปัญหา 2.3.10)
2. หนึ่งหน้าจอมีปุ่ม primary ได้ปุ่มเดียว
3. เส้นแบ่งใช้ต่อเมื่อสองโซนมีหน้าที่ต่างกันจริง ไม่ใช้ตกแต่ง
4. ห้ามมีสี hardcode ในไฟล์ component เด็ดขาด — ตรวจด้วย `grep -rnE "#[0-9a-f]{3,8}|white/|black/" src/renderer/components src/renderer/App.tsx` ต้องได้ผลลัพธ์ว่าง

---

## 6. สเปค Primitive

### 6.1 ตารางสถานะ 5 แบบ

| Component | default | hover | active | focus-visible | disabled |
|---|---|---|---|---|---|
| `Btn` primary | พื้น `accent-strong` ตัวอักษรขาว | สว่างขึ้น 8% | มืดลง 6% | ring 2px `accent` offset 2 | opacity .4 + cursor not-allowed |
| `Btn` default | พื้น `raised` ขอบ `line-strong` | พื้น `line`/12% | มืดลง | เหมือนกัน | เหมือนกัน |
| `Btn` ghost | โปร่ง ตัวอักษร `ink-muted` | พื้น `raised` ตัวอักษร `ink` | พื้น `line` | เหมือนกัน | เหมือนกัน |
| `Btn` danger | พื้น `danger`/10% ขอบ `danger`/40% | พื้น `danger`/20% | /28% | ring `danger` | เหมือนกัน |
| `TextInput` | พื้น `canvas` ขอบ `line-strong` | ขอบเข้มขึ้น | — | ขอบ `accent` + ring | พื้น `raised` ตัวอักษร `ink-disabled` |
| `Chip` | ขอบ `line-strong` พื้นโปร่ง | พื้น `raised` | — | ring | opacity .4 |
| `Chip` selected | ขอบ `accent` พื้น `accent-soft` ตัวอักษร `accent` | เข้มขึ้น | — | ring | — |
| `Toggle` off | ราง `line-strong` | ราง เข้มขึ้น | — | ring รอบราง | opacity .4 |
| `Toggle` on | ราง `accent-strong` หัวขาว | สว่างขึ้น | — | ring | — |
| `SegmentedControl` | ตัวอักษร `ink-muted` | `ink` | — | ring รอบตัวที่โฟกัส | opacity .4 |
| `SegmentedControl` active | พื้น `accent-strong` ตัวอักษรขาว | สว่างขึ้น | — | ring | — |

### 6.2 Prop ที่ต้องเพิ่ม

| Component | prop ใหม่ | ค่า | เหตุผล |
|---|---|---|---|
| `Btn` | `size` | `'sm' \| 'md' \| 'lg'` (default `md`) | ลบ `!px-2 !py-1 !text-xs` ทั้ง 8 จุด |
| `Btn` | `iconOnly` | `boolean` | ปุ่มไอคอนล้วนต้องเป็นสี่เหลี่ยมจัตุรัส + บังคับ `aria-label` |
| `Chip` | `size` | `'sm' \| 'md'` | รายการวันในแท็บ Schedule ต้องการตัวเล็ก |
| `Badge` | `size` | `'sm' \| 'md'` | เหมือนกัน |
| `Field` | `density` | `'compact' \| 'default'` | panel แคบ 280px ต้องบีบได้ |
| `SectionTitle` | `level` | `1 \| 2` | สร้างลำดับชั้น แทนที่จะตะโกนเท่ากันหมด |
| `ValueSlider` | `showInput` | `boolean` | slider hue/sat/light ต้องพิมพ์ค่าเป๊ะได้ |
| `SelectV2` | `searchable` | `boolean` | รายการฟอนต์ยาวเกินกว่าจะเลื่อนหา |

### 6.3 Primitive ใหม่ที่ต้องสร้าง

| ชื่อ | หน้าที่ | ใช้ที่ไหน |
|---|---|---|
| `Panel` | กล่องระดับบนของ panel มีหัวติดหนึบ + เนื้อหาเลื่อน | ทุกแท็บ |
| `Toolbar` | แถบเครื่องมือแนวนอน จัดกลุ่มซ้าย/กลาง/ขวา | preview toolbar, หัว panel |
| `IconButton` | ปุ่มไอคอนล้วน บังคับ `aria-label` และ tooltip | titlebar, rail, HUD ซูม |
| `EmptyState` | ไอคอน + ข้อความ + ปุ่มลงมือ | Drafts ว่าง, ยังไม่เลือกเทมเพลต, ยังไม่มีรูป |
| `NumberInput` | ช่องตัวเลขมีปุ่มเพิ่ม/ลด และรับ scroll | คู่กับทุก slider |
| `ThumbGrid` | กริดภาพย่อเลือกได้ + ตัวกรอง | แกลเลอรีเทมเพลต (ข้อ 4.2) |
| `Stack` / `Row` | จัดวางตามสเกลระยะ ปิดโอกาสใส่ค่ามั่ว | ทุกที่ |

### 6.4 Primitive ที่ต้องลบ

1. `ComingSoon` — ของที่ขายแล้วไม่ควรมีกล่อง "เร็ว ๆ นี้" อยู่ในตัวสินค้า ให้ซ่อนฟีเจอร์ที่ยังไม่เสร็จไปเลย
2. keyframe `card-flip-in` และคลาส `.nav-rail-btn` — ซากธีม Moon Priestess ที่กำลังจะถูกเอาออก
3. `--color-gold` — ลบหลังจากย้าย call site หมดแล้ว

---

## 7. โครงหน้าจอใหม่

### 7.1 ภาพรวม

```
┌───────────────────────────────────────────────────────────────┐
│ titlebar 36px  VSG │ ↶ ↷ │       บันทึกแล้ว 14:32 │ ☾ │ ─ □ ✕ │
├────┬────────────────────┬─────────────────────────────────────┤
│rail│ EditorPanel        │ 1920×1080 │ UI:TH การ์ด:TH │[⬇ Export]│
│48px│ 280–520px (ลากได้) ├─────────────────────────────────────┤
│    │ ┌────────────────┐ │▓▓▓▓▓▓▓▓▓▓ พื้น sunken ▓▓▓▓▓▓▓▓▓▓▓▓│
│ ⚙  │ │ หัวแท็บ (ติดหนึบ)│ │▓▓  ┌──────────────────────┐   ▓▓│
│ 📅 │ ├────────────────┤ │▓▓  │                      │   ▓▓│
│ 🎨 │ │                │ │▓▓  │   การ์ดตารางสตรีม     │   ▓▓│
│ 🖼 │ │  เนื้อหาเลื่อนได้│ │▓▓  │   + เงา stage        │   ▓▓│
│ ── │ │                │ │▓▓  └──────────────────────┘   ▓▓│
│ 💾 │ └────────────────┘ │▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓ [− 62% +] [⤢] ▓▓│
│ ?  │                    │                                     │
└────┴────────────────────┴─────────────────────────────────────┘
```

### 7.2 Titlebar (36px จาก 40px)

1. wordmark ย่อเหลือตัวย่อ + ชื่อเต็มบน tooltip
2. undo/redo เพิ่ม tooltip บอกว่าจะย้อน**อะไร** เช่น "ย้อน: เปลี่ยนเทมเพลต" ไม่ใช่แค่คำว่า "ย้อน"
3. `StatusPill` แสดงตลอด ไม่ใช่ `hidden sm:flex` — สถานะบันทึกคือข้อมูลที่ต้องเห็นเสมอ โดยเฉพาะกับผู้ใช้ที่ซื้อไปและยังไม่ไว้ใจแอป
4. **ย้ายปุ่มภาษา UI (🌐) ออกจาก titlebar** ไปรวมกับปุ่มภาษาการ์ดบน preview toolbar (แก้ปัญหา 2.3.9)
5. `titlebar-drag` / `titlebar-nodrag` ต้องอยู่ครบ ไม่งั้นลากหน้าต่างไม่ได้

### 7.3 IconRail (48px จาก 72px)

1. ไอคอนอย่างเดียว + tooltip ด้านขวา — ป้าย 10px อ่านยากและกินความกว้าง 24px
2. **ทรงเดียวกันทุกปุ่ม** เลิกแยกทรงไพ่ทาโรต์ (แก้ปัญหา 2.3.5) กลุ่มยังแยกด้วยเส้นคั่นที่มีอยู่แล้ว
3. ตัวชี้ว่าเลือกอยู่: แถบ 2px ชิดซ้าย + พื้น `accent-soft` ไม่ใช้ gradient
4. ปุ่ม Help ต้องทำงานจริง เปิดแผ่นคีย์ลัด + ลิงก์คู่มือ — **บังคับ เพราะจะขาย**
5. คีย์ลัด `Ctrl+1` ถึง `Ctrl+6` สลับแท็บ

### 7.4 EditorPanel (280–520px ลากได้)

1. ขอบลากได้ จำค่าลง `localStorage` คีย์ `vsg:panelWidth`
2. ดับเบิลคลิกขอบ = รีเซ็ตกลับ 380px
3. หัว panel ติดหนึบ บอกว่าอยู่แท็บไหน
4. โครงเนื้อหาทุกแท็บเหมือนกัน: `หัวแท็บ → กลุ่ม → ฟิลด์` ไม่เกิน 3 ชั้น
5. ที่ 280px ต้องยังอ่านภาษาไทยได้ครบ — ทดสอบด้วยภาษาไทย ไม่ใช่อังกฤษ

### 7.5 พื้นที่พรีวิว — ส่วนที่เปลี่ยนเยอะที่สุด

1. **แถบเครื่องมือบน:** ขนาด output · ตัวเลือกภาษา UI และภาษาการ์ดที่จัดกลุ่มพร้อมป้ายกำกับ · **ปุ่ม Export หลักดักไว้ตรงนี้** (แก้ปัญหา 2.3.15 + เป้าหมาย 4.1.4)
2. **พื้นโรงถ่าย `sunken`** + ตารางหมากรุกรอบการ์ด ดูขอบโปร่งใสได้จริง + เงา `--shadow-stage` ใต้การ์ด นี่คือภาพจำของแอป (ข้อ 3.4.3)
3. **HUD ซูมมุมขวาล่าง:** `−` `62%` `+` `⤢ พอดีจอ` — คลิกตัวเลขเพื่อพิมพ์ค่าเอง
4. **คีย์ลัด:** `Ctrl+0` พอดีจอ · `Ctrl+Alt+1` ขนาดจริง 100% · `Space+ลาก` เลื่อน · `Ctrl+ล้อ` ซูม
5. **ห้ามแก้สัญญาของ `ScheduleStage`** — `StageScaler` ยังส่ง prop `previewScale` แบบเดิมเป๊ะ การซูมทำที่ชั้น `StageScaler` เท่านั้น `components/canvas/` ไม่ถูกแตะ
6. สวิตช์ "แสดงขอบเขตพื้นที่ปลอดภัย" ดูว่าข้อความชิดขอบเกินไปไหม

### 7.6 การแก้วันในสัปดาห์ — modal → แผงข้าง

1. คลิกวันบนการ์ดหรือในรายการ → panel สลับเป็นโหมด "แก้วันจันทร์" **พรีวิวยังเห็นเต็ม ๆ**
2. ขณะแก้ วันนั้นบนการ์ดถูกไฮไลต์ด้วยกรอบ accent
3. ปุ่ม `‹ ›` เลื่อนไปวันก่อนหน้า/ถัดไปโดยไม่ต้องปิดกลับ
4. `Esc` ออก, `Ctrl+Enter` ยืนยันแล้วไปวันถัดไป
5. ลบ `RDialog` ออกจาก `DayEditPanel` — เนื้อหาข้างในเก็บไว้เกือบทั้งหมด แค่ย้ายที่อยู่
6. **นี่คือการเปลี่ยนที่คุ้มที่สุดในแผนทั้งหมด** ถ้าจะทำอย่างเดียว ให้ทำอันนี้

---

## 8. รายละเอียดรายแท็บ

### 8.1 General

1. ยุบ 2 section เหลือหน้าเดียวไม่มีหัวข้อ: ชื่อ VTuber · เครดิตศิลปิน · รูปแบบวันที่ · รูปแบบเวลา
2. เพิ่มพรีวิวสดของรูปแบบวันที่ ("จ. 1 ก.ย." ไม่ใช่แค่ชื่อรูปแบบ)

### 8.2 Schedule

1. `WeekPicker` เพิ่มปุ่ม "สัปดาห์นี้ / สัปดาห์หน้า" กดครั้งเดียว
2. รายการ 7 วันเปลี่ยนจากการ์ดสูงเป็นแถวกะทัดรัด: `จ. 1 ก.ย. · 20:00 · Twitch · ชื่อสตรีม` + ป้ายสถานะ
3. แถวที่กำลังแก้ = สถานะ selected ชัดเจน ไม่ใช่แค่ hover
4. `SegmentedControl` สถานะด่วน ย้ายเข้าไปในโหมดแก้วัน ไม่ต้องโชว์ทั้ง 7 แถวพร้อมกัน
5. เพิ่มปุ่ม "คัดลอกจากสัปดาห์ก่อน" — ตรงกับเป้าหมาย 4.1.1

### 8.3 Template (เดิมชื่อ Theme) — แท็บที่เปลี่ยนโครงมากที่สุด

1. **บนสุด: แกลเลอรีเทมเพลต** ตามข้อ 4.2 — ของแรกที่เห็น
2. ถัดมา 3 accordion เปิดได้ทีละอัน (`type="single"`):
   - **โทนสี** — preset + slider hue/sat/light + `NumberInput` + ปุ่มรีเซ็ต
   - **องค์ประกอบ** — **สร้างจาก `controls` ของเทมเพลต ไม่ hardcode** (ข้อ 4.3.4)
   - **ฟอนต์** — 3 ช่อง + FontSourcePicker
3. ตัวอย่างโทนสีเป็นแถบสีจริง ไม่ใช่ชื่อ
4. `FontSourcePicker` แสดงตัวอย่างข้อความไทย/อังกฤษ/ญี่ปุ่นในฟอนต์นั้นจริง

### 8.4 Assets

1. รูปตัวละครแสดงเป็นภาพย่อพร้อมกรอบ ไม่ใช่ชื่อไฟล์
2. slider transform 3 ตัวได้ปุ่มรีเซ็ตรายตัว
3. `DropZone` ใช้โหมด `compact` เมื่อมีรูปแล้ว
4. เพิ่มปุ่ม "จัดกึ่งกลาง" และ "พอดีกรอบ"

### 8.5 Export

1. preset เปลี่ยนจาก dropdown เป็นการ์ดเลือกที่บอกขนาดจริง + อัตราส่วน
2. ปุ่ม export หลักอยู่บน preview toolbar (7.5.1) ในแท็บเหลือแค่ตัวเลือก
3. ระหว่าง export ต้องมีแถบความคืบหน้าจริง ไม่ใช่แค่ spinner (GIF ใช้เวลานาน)
4. เสร็จแล้วแสดงเส้นทางไฟล์ + ปุ่ม "เปิดโฟลเดอร์"
5. จำค่า export ล่าสุดเป็นค่าเริ่มต้นครั้งถัดไป

### 8.6 Drafts

1. รายการมีภาพย่อ — ตอนนี้เป็นข้อความล้วน จำไม่ได้ว่าอันไหนคืออันไหน
2. เพิ่มช่องค้นหา + เรียงตาม (แก้ล่าสุด / ชื่อ)
3. ลบ override `!px-2 !py-1 !text-xs` ทั้งหมด ใช้ `<Btn size="sm">`
4. ลบ draft ต้องมีขั้นยืนยัน — ตอนนี้กดแล้วหายเลย
5. `EmptyState` เมื่อยังไม่มี draft พร้อมปุ่ม "บันทึกงานปัจจุบันเป็นแบบร่าง"

---

## 9. Motion และการเข้าถึง

1. เก็บ keyframe `fade-in`, `toast-in`, `sheet-up` · ลบ `card-flip-in`
2. ระยะเวลา: 120ms (ปุ่ม/hover) · 180ms (panel/accordion) · 240ms (modal/sheet) ห้ามเกิน
3. บล็อก `prefers-reduced-motion` ถูกต้องแล้ว เก็บไว้ทั้งดุ้น
4. **แก้ focus ring:** เอา `border-radius: 10px` ออกจาก `:focus-visible` ใช้ `border-radius: inherit` (แก้ปัญหา 2.3.16)
5. ทุกปุ่มไอคอนล้วนต้องมี `aria-label` — ตอนนี้ปุ่มควบคุมหน้าต่างมีแค่ `title`
6. contrast ตามตาราง 5.3 — ตรวจซ้ำด้วยเครื่องมือก่อน merge ทุกครั้ง
7. เป้าคลิกขั้นต่ำ 32×32px (ปัจจุบัน undo/redo คือ 28×28)
8. เดินครบทุก flow ด้วยคีย์บอร์ดอย่างเดียวได้: เลือกเทมเพลต → แก้ทุกวัน → เปลี่ยนโทนสี → export

---

## 10. แผนการทำเป็นเฟส

### เฟส 0 · วางหลักฐานตั้งต้น (ครึ่งวัน) — ไม่มีอะไรเปลี่ยนหน้าตา

1. export PNG 3 ใบ + GIF 1 ใบ เก็บ hash ใน `docs/baseline-hashes.txt`
2. ถ่ายภาพหน้าจอทุกแท็บ ทั้งสว่างและมืด เก็บใน `docs/before/`
3. `npm run lint` ผ่านสะอาด
4. `git tag ui-baseline`
5. **เกณฑ์ผ่าน:** แอปเปิดได้ export ได้ ไม่มีอะไรเปลี่ยน

### เฟส 1 · ชั้น Token (1 วัน) — ได้ผลทางสายตา 60% โดยไม่แตะ component เลย

1. เขียนบล็อก `@theme` และ `.dark` ใหม่ด้วยค่าจากข้อ 5.1–5.2 ทั้งชุด
2. `--color-gold` ชี้ไปที่ `accent-soft` ไว้ก่อน ยังไม่ลบ
3. แทนสี hardcode: `grep -rn "white/\|#3c3860\|black/60" src/renderer`
4. แก้ `:focus-visible` ตามข้อ 9.4
5. **เกณฑ์ผ่าน:** ทุกแท็บเปิดได้ ไม่มีข้อความหาย contrast ตรงตาราง 5.3 · export hash ตรงกับเฟส 0
6. **ย้อนกลับ:** revert ไฟล์เดียว

### เฟส 2 · Primitive (2 วัน)

1. เพิ่ม prop ตามตาราง 6.2 แล้วไล่ลบ `!` override ทุกจุด
2. สร้าง primitive ใหม่ตามตาราง 6.3 (ยังไม่ต้องใช้ทุกตัว)
3. ลบตามข้อ 6.4: `ComingSoon`, `card-flip-in`, `.nav-rail-btn`, `--color-gold`
4. เปลี่ยนขอบ interactive จาก `border-line` เป็น `border-line-strong` ทุกจุด (ข้อ 5.3.1)
5. **เกณฑ์ผ่าน:** ไม่เหลือ `!` override ใน `components/editor/**` · `grep` สี hardcode ได้ผลว่าง · export hash ยังตรง

### เฟส 3 · โครงหน้าจอ (3 วัน) — อยู่หลังสวิตช์ ปิดได้ทันที

1. สวิตช์ `localStorage['vsg:ui'] = 'v1' | 'v2'` ค่าเริ่มต้น `v1`
2. เขียน `AppShell.tsx` ใหม่ตามข้อ 7.1–7.5 ไม่ทับ `App.tsx` เดิม
3. `App.tsx` เหลือหน้าที่เดียว: เลือกว่าจะเรนเดอร์ shell เก่าหรือใหม่
4. รวมสาขา mobile/desktop เหลือชุดเดียว ลบ `mobileMore` ที่ตายแล้ว
5. `StageScaler` เพิ่มซูม/เลื่อน — **prop `previewScale` ต้องเหมือนเดิมทุกตัวอักษร**
6. ทดสอบลากหน้าต่างจริงหลังแตะ titlebar
7. **เกณฑ์ผ่าน:** สลับ v1/v2 ได้ ทั้งคู่ใช้งานได้ · ลาก/ย่อ/ขยาย/ปิดหน้าต่างได้ · export hash ยังตรง
8. **ย้อนกลับ:** ตั้งค่ากลับเป็น `v1` ไม่ต้อง build ใหม่

### เฟส 3.5 · เปิดทางให้ 10 เทมเพลต (1 วัน) — **เฟสใหม่ เกิดจากคำตอบข้อ 6**

1. ทำให้ชิ้นส่วนที่ไม่ใช่แกนกลางเป็น optional ใน `layout.schema.ts` (ข้อ 4.3.1–4.3.2)
2. เพิ่มฟิลด์ `controls?: string[]` — ไม่มีก็ถือว่าโชว์ทุกอย่าง
3. `ScheduleStage` ข้ามการวาดชิ้นส่วนที่เทมเพลตไม่ประกาศ แทนที่จะพังหรือวาดรูปเปล่า
4. **นี่คือจุดเดียวที่แตะ `components/canvas/`** — ต้องยืนยันด้วย hash ว่า Sakura Diary ยังออกมาเหมือนเดิมเป๊ะ ถ้าไม่ตรง หยุดทันที
5. **เกณฑ์ผ่าน:** เทมเพลตเดิมยังเรนเดอร์เหมือนเดิม hash ตรง · ใส่เทมเพลตทดสอบที่ไม่มี `weekRibbon` แล้วไม่พัง

### เฟส 4 · รายแท็บ (3 วัน) — merge ได้ทีละแท็บ

1. ลำดับ: Drafts → Assets → General → Export → **Template** → Schedule (ง่ายไปยาก)
2. **Schedule ทำท้ายสุด** เพราะรวมงานย้าย `DayEditPanel` เป็นแผงข้าง ซึ่งเสี่ยงที่สุด
3. **Template ต้องทำหลังเฟส 3.5** เพราะต้องอ่าน `controls`
4. `i18n/translations.ts` เพิ่มคีย์ได้ **ห้ามลบคีย์เก่า** จนจบเฟส
5. **เกณฑ์ผ่าน (ทุกแท็บ):** ทุกฟิลด์เขียนลง store ได้เหมือนเดิม · undo/redo ทำงาน · autosave ทำงาน · ครบทั้ง TH/EN · export hash ยังตรง

### เฟส 5 · งานที่ "จะขาย" บังคับต้องมี (2 วัน) — **เลื่อนขึ้นจากเดิมที่เป็นของเสริม**

1. **first-run flow** — เปิดครั้งแรกเจอแกลเลอรีเทมเพลต ไม่ใช่ฟอร์มเปล่า (เป้าหมาย 4.1.1)
2. **`EmptyState` ครบทุกที่** — Drafts ว่าง, ยังไม่มีรูปตัวละคร
3. **ปุ่ม Help ที่ทำงานจริง** + แผ่นคีย์ลัด
4. **ข้อความ error ที่คนนอกอ่านรู้เรื่อง** — ตรวจทุกข้อความใน `ErrorBoundary` และ toast
5. เหตุผลที่เลื่อนขึ้น: ข้อ 2 ตอบว่าจะขาย ผู้ใช้จะไม่มีใครสอน ของสามอย่างนี้เลยกลายเป็นของบังคับ ไม่ใช่ของเสริม

### เฟส 6 · ของเสริมจริง ๆ (ทำหรือไม่ทำก็ได้)

1. command palette (`Ctrl+K`)
2. โหมดกะทัดรัดผ่านตัวแปร `--density`
3. accent ดึงจาก `palette.dayOnline` ของเทมเพลต เฉพาะแถบพรีวิว (ข้อ 3.4.5)

**รวมเวลา:** เฟส 0–5 = **12.5 วันทำงาน** (เดิม 9.5 วัน เพิ่ม 3 วันจากเฟส 3.5 และเฟส 5 ที่กลายเป็นงานบังคับ)

---

## 11. แผนที่ไฟล์

| ไฟล์ | เฟส | เนื้องาน |
|---|---|---|
| `src/renderer/index.css` | 1 | `@theme` + `.dark` ใหม่ทั้งชุด, ลบสี hardcode, แก้ focus ring |
| `src/renderer/components/editor/ui.tsx` | 2 | prop `size`/`density`/`level`, primitive ใหม่, ลบซากเก่า |
| `src/renderer/AppShell.tsx` *(ใหม่)* | 3 | โครงหน้าจอใหม่ทั้งหมด |
| `src/renderer/App.tsx` | 3 | เหลือแค่ตัวเลือก shell |
| `src/renderer/components/editor/IconRail.tsx` | 3 | 48px, ทรงเดียว, tooltip, Help ที่ทำงาน |
| `src/renderer/components/editor/EditorPanel.tsx` | 3 | ลากปรับความกว้าง + หัวติดหนึบ |
| `src/renderer/template/layout.schema.ts` | **3.5** | ชิ้นส่วน optional + ฟิลด์ `controls` |
| `src/renderer/components/canvas/ScheduleStage.tsx` | **3.5** | ข้ามชิ้นส่วนที่เทมเพลตไม่ประกาศ — **จุดเสี่ยงสูงสุด ต้องยืนยันด้วย hash** |
| `src/renderer/components/editor/tabs/*.tsx` | 4 | ตามข้อ 8 |
| `src/renderer/components/editor/DayEditPanel.tsx` | 4 | modal → แผงข้าง |
| `src/renderer/components/editor/WeekPicker.tsx` | 4 | ปุ่มลัดสัปดาห์ |
| `src/renderer/components/editor/FontSourcePicker.tsx` | 4 | ตัวอย่างฟอนต์จริง |
| `src/renderer/components/editor/toast.tsx` | 4 | token ใหม่ |
| `src/renderer/i18n/translations.ts` | 1–5 | เพิ่มคีย์เท่านั้น ห้ามลบ |
| `public/templates/<id>/thumb.webp` *(ใหม่)* | 4 | ภาพย่อเทมเพลต 10 ใบ |
| `src/App.css` | 2 | ตรวจว่ายังถูกใช้ไหม ถ้าไม่ ลบทิ้ง |

### 11.1 ไฟล์ที่ห้ามแตะ

1. `src/renderer/store/**` — scheduleStore, themeStore, textStyleStore, history, autosave
2. `src/renderer/export/**` — exporter
3. `src/renderer/utils/**` — ยกเว้นเพิ่มไฟล์ใหม่ (เช่น `utils/zoom.ts`)
4. `src/main/**`, `src/preload/**` — ฝั่ง Electron
5. `vite.config.ts`, `package.json` — แผนนี้ไม่ต้องเพิ่ม dependency ใด ๆ
6. `components/canvas/**` — ยกเว้น `ScheduleStage.tsx` ในเฟส 3.5 เท่านั้น

---

## 12. ความเสี่ยงและวิธีรับมือ

1. **เฟส 3.5 แตะ `ScheduleStage`** — นี่คือความเสี่ยงอันดับหนึ่งของแผนใหม่ ทุก commit ต้องรัน hash เทียบ ถ้าไม่ตรงแม้แต่ไบต์เดียว ย้อนทันทีอย่าเดา
2. **ความยาวข้อความไทย/ญี่ปุ่นล้นเลย์เอาต์แคบลง** — panel แคบได้ถึง 280px ต้องทดสอบทุกหน้าจอด้วยภาษาไทยก่อน merge ไม่ใช่ทดสอบอังกฤษแล้วเดา
3. **เปลี่ยนชื่อ token แล้วคลาสเก่าพัง** — Tailwind 4 ไม่เตือนตอน build ถ้าใช้ชื่อ token ที่ไม่มี มันจะเงียบ ๆ ไม่ใส่สี ต้อง `grep -rn "gold\|accent-strong" src/` ก่อนเปลี่ยนทุกครั้ง
4. **การซูมทำให้ Konva วาดผิดขนาด** — `StageScaler` คำนวณจาก `ResizeObserver` ของกล่องพ่อ ถ้าเพิ่มซูม/เลื่อนแล้วกล่องเปลี่ยนขนาดจะวนซ้ำ แก้ด้วยการแยกกล่องวัดขนาดกับกล่องที่ถูก transform ออกจากกัน
5. **Radix portal หลุด token** — `.radix-tooltip`, `.radix-select-content`, `.radix-popover` อยู่ใน `index.css` และ render นอก DOM tree ปกติ ต้องอัปเดตพร้อม token ไม่งั้น dropdown จะยังสีเก่า
6. **ลากหน้าต่างไม่ได้หลังรื้อ shell** — Electron frameless พึ่ง `-webkit-app-region: drag` ล้วน ๆ เผลอลบคลาสจะลากไม่ได้และแก้จากในแอปไม่ได้ ทดสอบด้วยมือทุกครั้ง
7. **`vsg:theme` ใน localStorage** — ต้องคงชื่อเดิม ไม่งั้นผู้ใช้เดิมเด้งกลับธีมสว่างโดยไม่รู้สาเหตุ
8. **modal → แผงข้าง ทำ state หาย** — `DayEditPanel` พึ่ง `selectedDay !== null` เป็นตัวเปิด/ปิด และ `App.tsx` มี effect สลับแท็บอัตโนมัติ ต้องย้ายทั้งคู่พร้อมกัน
9. **draft เก่าอ้างเทมเพลตที่ถูกลบ** — `getTemplate` มี fallback `?? TEMPLATES[0]` อยู่แล้ว จึงไม่พัง **แต่ผู้ใช้จะงงว่าทำไมงานเก่าหน้าตาเปลี่ยน** ต้องมี toast แจ้ง ไม่ใช่สลับเงียบ ๆ
10. **ภาพย่อเทมเพลต 10 ใบทำให้ไฟล์ติดตั้งใหญ่ขึ้น** — ใช้ webp ขนาด ~320px กว้าง ควรอยู่ราวใบละ 20–40KB รวมไม่เกิน 400KB ถ้าเกินให้ลดคุณภาพ ไม่ใช่ลดจำนวน

---

## 13. เช็กลิสต์ตรวจก่อน merge ทุกเฟส

1. `npm run lint` ผ่านสะอาด
2. `tsc -b` ไม่มี error
3. export PNG แล้ว hash ตรงกับ `docs/baseline-hashes.txt`
4. export GIF ได้ ไม่ค้าง
5. undo/redo ทำงานครบทุกช่องที่แก้ไข
6. autosave ทำงาน — ปิดแอปแล้วเปิดใหม่ ข้อมูลอยู่ครบ
7. โหลด draft เก่าที่บันทึกก่อน redesign ได้
8. สลับสว่าง/มืดแล้วไม่มีข้อความหายหรือ contrast ตก
9. สลับ TH/EN แล้วไม่มีข้อความล้นกล่อง — ทดสอบที่ panel 280px ด้วย
10. ลากหน้าต่าง ย่อ ขยาย ปิด ได้ครบ
11. เดินครบทุก flow ด้วยคีย์บอร์ดอย่างเดียว
12. ย่อหน้าต่างเหลือกว้าง 900px แล้วยังใช้งานได้
13. **นับคลิกจากเปิดแอปถึง export ครั้งแรก ต้อง ≤ 5** (ตั้งแต่เฟส 5 เป็นต้นไป)

---

## 14. ขั้นตอนถัดไป

1. **ตอบคำถามข้อ 4.4** (โทนสีเป็นของกลางหรือของแต่ละเทมเพลต) — ต้องตอบก่อนเริ่มเฟส 3.5 เท่านั้น ไม่บล็อกเฟส 0–3
2. **เริ่มเฟส 0 และ 1 ได้ทันที** ไม่ต้องรออะไร เฟส 1 ให้ผลทางสายตามากที่สุดต่อแรงที่ลง
3. ถ้าอยากได้ผลเร็วที่สุดโดยลงแรงน้อยที่สุด: **เฟส 1 + ข้อ 7.6 (modal → แผงข้าง)** สองอย่างนี้ให้ความรู้สึกราว 70% ของแผนทั้งหมด ใช้เวลาประมาณ 3 วัน
4. เฟส 3.5 คือทางแยก — ถ้าตัดสินใจว่าจะไม่ทำ 10 เทมเพลตแล้ว ให้ข้ามเฟสนี้และคงข้อห้ามแตะ `components/canvas/` ไว้ตามเดิม
